diff --git a/setup.py b/setup.py
index c854d92..1e620a4 100644
--- a/setup.py
+++ b/setup.py
@@ -7,16 +7,28 @@ https://packaging.python.org/en/latest/distributing.html
 https://github.com/pypa/sampleproject
 """
 
-from Cython.Build import cythonize
 import numpy as np
-from Cython.Distutils import build_ext
 from setuptools import Extension, setup
+from setuptools.command.build_ext import build_ext
+from pathlib import Path
+
+source_path = Path("KDEpy/cutils_ext")
+pyx_file = source_path / "cutils.pyx"
+c_file = source_path / "cutils.c"
+
+if pyx_file.exists():
+    from Cython.Build import cythonize
+
+    ext_modules = cythonize(
+        [Extension("KDEpy._cutils", [str(pyx_file)])],
+    )
+else:
+    ext_modules = [
+        Extension("KDEpy._cutils", [str(c_file)], include_dirs=[np.get_include()])
+    ]
 
 setup(
     packages=["KDEpy"],
     cmdclass={"build_ext": build_ext},
-    include_dirs=[np.get_include()],
-    ext_modules=cythonize(
-        [Extension("KDEpy._cutils", ["KDEpy/cutils_ext/cutils.pyx"])],
-    ),
+    ext_modules=ext_modules,
 )
